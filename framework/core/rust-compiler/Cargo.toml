[package]
name = "fusee-compiler"
version = "0.1.0"
edition = "2021"
description = "Fusée template compiler — lexer/parser/transformer/generator — Rust+WASM port"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
serde-wasm-bindgen = "0.6"
console_error_panic_hook = { version = "0.1", optional = true }
wee_alloc = { version = "0.4", optional = true }
once_cell = "1"
regex = "1"
serde_json = "1"

[features]
default = ["console_error_panic_hook"]
wee_alloc = ["dep:wee_alloc"]

[profile.release]
opt-level = "s"
lto = true