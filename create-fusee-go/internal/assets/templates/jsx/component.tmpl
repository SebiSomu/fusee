import { defineComponent } from 'fusee-framework';

export const {{.ProjectName}} = defineComponent({
    setup() {
        return () => (
            <div class="component-{{.ProjectName}}">
                <h3>{{.ProjectName}} Component</h3>
            </div>
        );
    }
});
